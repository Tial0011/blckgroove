/* ==========================================================================
   BLCKGROOVE — Entry point
   Phase 1 only boots the navbar. Future phases will initialise hero,
   gallery, and other section modules from here.
   ========================================================================== */

document.addEventListener('DOMContentLoaded', () => {
  loadNavbar();
  loadFooter();
});
